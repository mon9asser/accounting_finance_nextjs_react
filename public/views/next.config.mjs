/** @type {import('next').NextConfig} */
const nextConfig = {
    trailingSlash: true,  
    images: {
        domains: ['codedtag.com'], // Add your domain here
    },    
};

export default nextConfig;
